# Задача A — Сумма двух чисел

## Идея решения
Считываем два целых числа `a` и `b` и выводим их сумму `a + b`.

Так как по условию `-10^9 ≤ a, b ≤ 10^9`, сумма лежит в диапазоне `[-2·10^9, 2·10^9]`,
что помещается в тип `int`.

## Сложность
- Время: O(1)
- Память: O(1)